import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-donor-home',
  imports: [RouterLink],
  templateUrl: './donor-home.html',
  styleUrl: './donor-home.css',
})
export class DonorHome {}